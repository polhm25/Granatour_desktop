# Configuración de Base de Datos para Docker LAMP Stack
# =====================================================

# Conexión MySQL
db.url=jdbc:mysql://localhost:3307/grana_tour?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
db.user=root
db.password=root_password

# Parámetros opcionales
db.driver=com.mysql.cj.jdbc.Driver
db.pool.size=10
db.connection.timeout=30000
